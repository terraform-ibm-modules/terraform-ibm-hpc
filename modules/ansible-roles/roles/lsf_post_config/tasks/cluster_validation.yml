---
- name: MTU Check | Restart NetworkManager if MTU {{ mtu_value }} is not configured
  ansible.builtin.shell: |
    ip route show | grep -q 'mtu {{ mtu_value }}' || {
      systemctl restart NetworkManager
      echo "restarted"
    }
  register: mtu_check_result
  changed_when: "'restarted' in mtu_check_result.stdout"
  when: inventory_hostname in groups['mgmt_compute_nodes']

- name: LSF Version | Retrieve lsid output
  become: true
  become_user: lsfadmin
  become_flags: "-i"
  ansible.builtin.shell: lsid
  register: lsid_output
  changed_when: false
  when: inventory_hostname == groups['management_nodes'][0]

- name: LSF Version | Display lsid output
  ansible.builtin.debug:
    msg: "{{ lsid_output.stdout }}"
  when: inventory_hostname == groups['management_nodes'][0]

- name: Cluster Status | Fetch node status using bhosts
  become: true
  become_user: lsfadmin
  become_flags: "-i"
  ansible.builtin.shell: bhosts -w
  register: cluster_status_output
  changed_when: false
  when: inventory_hostname == groups['management_nodes'][0]

- name: Cluster Status | Show node status
  ansible.builtin.debug:
    msg: "{{ cluster_status_output.stdout }}"
  when: inventory_hostname == groups['management_nodes'][0]

- name: Ensure login node is registered in LSF
  when: inventory_hostname == groups['management_nodes'][0]
  become: true
  become_user: lsfadmin
  block:

    - name: Check if login node exists in LSF hosts list
      ansible.builtin.shell: |
        source /opt/ibm/lsf/conf/profile.lsf
        lshosts -w | grep login
      args:
        executable: /bin/bash
      register: login_host_check
      failed_when: false
      changed_when: false

    - name: Run LSF reconfig if login node is missing
      ansible.builtin.shell: |
        sudo service lsfd restart
      args:
        executable: /bin/bash
      when:
        - login_host_check.rc != 0

    - name: Wait for LSF to stabilize
      ansible.builtin.pause:
        seconds: 10
      when: login_host_check.rc != 0

  rescue:
    - name: Log failure
      ansible.builtin.debug:
        msg: "Failed to ensure login node is registered in LSF"
