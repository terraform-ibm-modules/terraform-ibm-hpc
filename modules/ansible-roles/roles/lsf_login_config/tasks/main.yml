---
# Configure Login Node
# Includes tasks to set up the login node configuration.

- name: Configure Login Node
  import_tasks: configure_login_node.yml

# Resize disk partition and filesystem
- name: Resize root disk
  include_role:
    name: common_resize_disk
