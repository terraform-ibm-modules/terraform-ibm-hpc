---

# setup dns resolution
- import_tasks: dns_resolution.yml

# setup dns resolution (delegating to lsf_deployer_hostname)
- import_tasks: dns_resolution.yml
  delegate_to: "{{ lsf_deployer_hostname }}.comp.com"

# install required prerequisite packages for the LSF cluster creation.
- import_tasks: lsf_prepare.yml

# tasks file for template tasks
- import_tasks: lsf_inventory.yml

# Create lsfadmin user
- import_tasks: lsfadmin_creation.yml

# Install Python dependencies
- import_tasks: python_installation.yml